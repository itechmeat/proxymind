# Part I

## Chapter 1

ProxyMind builds knowledge memories from long-form documents. This paragraph repeats stable text to force realistic token volume for hierarchy qualification. ProxyMind builds knowledge memories from long-form documents. This paragraph repeats stable text to force realistic token volume for hierarchy qualification. ProxyMind builds knowledge memories from long-form documents. This paragraph repeats stable text to force realistic token volume for hierarchy qualification.

## Chapter 2

Structured sections should become parent chunks when the document is long enough. Structured sections should become parent chunks when the document is long enough. Structured sections should become parent chunks when the document is long enough. Structured sections should become parent chunks when the document is long enough. Structured sections should become parent chunks when the document is long enough.

## Chapter 3

Retrieval should keep child ranking but allow the prompt builder to include deduplicated parent context. Retrieval should keep child ranking but allow the prompt builder to include deduplicated parent context. Retrieval should keep child ranking but allow the prompt builder to include deduplicated parent context. Retrieval should keep child ranking but allow the prompt builder to include deduplicated parent context.
