# Optional in development when Vite proxies `/api` to the local backend.
# Set this in production or when the frontend and backend use different origins.
VITE_API_URL=http://localhost:8000
VITE_DEFAULT_LANGUAGE=en
VITE_TWIN_NAME=ProxyMind
VITE_TWIN_AVATAR_URL=
VITE_ADMIN_MODE=true
